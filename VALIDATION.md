# 검증 기록

## 공개 배포 확인 (2026-09-20)

- GitHub에 0.5.1 Setup·ZIP·소스와 해시를 공개했습니다. 실제 Setup 다운로드의 SHA-256이 원본과 일치합니다.
- 운영 웹의 로그인·검사 코드 발급과 초대 화면의 Setup 직접 다운로드 링크를 확인했습니다.
- 앱과 동일한 .NET 검사 엔진·API 클라이언트로 생성 표본 3개를 검사하고 실제 Cloudflare 서버에 제출했습니다. 운영자 웹에서 정상 JAR, 반복 클릭 검토 대상, 손상 ZIP 오류와 부분 완료 상태를 확인했습니다. 이 시험은 WPF 버튼 조작이나 다른 PC에서의 실행 시험을 대신하지 않습니다.
- [Windows CI](https://github.com/Junnior123/Sentinel/actions/runs/35479479916)에서 웹·API·검사 엔진 테스트, Setup 생성, 설치·실행·재설치·제거와 사용자 설정 보존이 통과했습니다.
- Windows 11 실기기·다른 PC의 전체 흐름, 관리자 권한 수집과 대규모 검사 성능의 추가 검증이 남아 있습니다. 아래 기록은 각 버전 당시의 검증 결과입니다.

## Sentinel 0.3.1 추가 검증

C# 88개, API·계약 14개 검증 통과. 동일 표본에서 파일 검사 시간 약 24%, 메모리 할당량 약 89% 감소. 전체 드라이브 속도 보장은 아닙니다. 측정 조건과 결과 (이전 릴리스 기록)를 참고하세요.

## Sentinel 0.3 검증 (2026-09-15)

- Setup 패키지: Inno Setup 6.7.3로 한국어·영문 설치 파일 생성. 별도 작업 폴더에서 실제 설치→시작 메뉴/제거 등록→재설치→서버 설정 보존→앱 실행→실행 중 설치 차단→제거→사용자 파일 보존 검증 통과. Windows 10 x64에서 시험했으며 Windows 11 실기기 검증과 코드 서명은 별도입니다.

- C# 84개 검증 통과: 목록 준비와 파일 진행률 분리, 실제 개수 기반 백분율, ETA 준비·계산·빈 목록, 취소, 정확한 제외 경로와 Temp 포함, 다른 사용자 프로필 경로 마스킹, Sodium/AppleSkin 이름 위장·재압축 탐지.
- 기존 정상 100개 합성 JAR와 실제 .NET DLL 100개에서 탐지 0, 공식 양성 11개 표본 검사 유지. 표본 결과는 실제 전체 탐지율이 아닙니다.
- API/계약 13개 테스트 통과: 공개 요약 명시 동의, 소유자 접근 제어, 개인정보 필드 제외, 익명 조회, 링크 교체·철회·만료·보고서 삭제 연동.
- 실제 WPF 앱 초대→동의→11개 공식 표본 검사→제출 통과. Edge에서 보고서 탭·검토·900px 레이아웃, 초대 파일, 익명 요약 공유→철회 통과. TypeScript/Vite 빌드와 Worker 배포 dry-run 통과(실제 업로드 없음).
- 최적화: 내부 파일 해시 대상 준비를 검사당 한 번 수행, 클래스 구조 검증은 유지하면서 필요한 규칙이 없으면 문자열 디코딩 생략, 진행 UI 갱신 200ms 제한, 반복 누락 원인 집계. 전체 드라이브 속도 향상 배율은 아직 측정하지 않았습니다.
- 파일 목록 수집은 사용자·다운로드·AppData·Temp를 Windows 폴더보다 우선합니다. 한도 이후 항목은 미검사이며 부분 완료로 표시합니다.
- 공개 Cloudflare/GitHub OAuth, 실제 Windows 11 배포, 전체 C 드라이브 성능·장시간 실행 시험은 아직 남아 있습니다. 지금은 로컬 검증 단계입니다.

## 이전 버전 검증 기록

# 검증 기록 — 2026-09-14

대상: Watchblock 0.2.0 / 규칙 `2026.09.14-policy.4`. **개발 검증판**이며 아래 표본 결과는 실제 전체 탐지율이 아닙니다. 이전 버전 기록은 아래에 남겨 두었습니다.

## 0.2 확장 검사 검증

- C# 69개 단언: 전체 확장자 목록·해시, 일반 문서의 매크로 오탐 방지, 공식 양성 표본 11개 식별과 원본 보존, 기존 압축·취소·정상 파일 회귀 포함.
- 새 양성: Advanced XRay Fabric·NeoForge, Spectator Xray 팩, Freecam, Xaero's Minimap, JourneyMap. 정확한 버전과 SHA-256은 `samples/manifest.json`에 기록.
- Spectator Xray ZIP 재압축과 압축 해제 폴더에서 모델 3개 해시 조합 일치. 정상 Anti Xray 방어용 팩은 알려진 치트 0·검토 0 (`samples/negative-manifest.json`).
- 실제 정상 .NET DLL 100개 및 합성 정상 JAR 100개에서 탐지 0. 이 표본은 모든 정상 소프트웨어를 대표하지 않음.
- 하드웨어 기준의 동일/변경 비교, 실제 Windows 펌웨어·설치 식별값·코드 무결성·네트워크 주소 재정의 설정 조회와 누락 보고 확인. 원본 식별값·해시는 보고서에 포함되지 않음.
- 12개 API·계약 테스트: 확장 검사를 지원하지 않는 앱은 코드 소비 전에 거부, 시스템 검토 항목의 파일 ID null 허용, 기존 권한·만료·10MB 검증 포함.
- WPF: 전체 파일 검사 초대 연결 → 동의 전 시작 차단 → 공식 표본 11개 검사 → 웹 제출 통과. 최대화·축소 화면을 확인.
- 웹: 결과가 검사 목록 아래에 길게 붙는 대신 별도 전체 너비 화면으로 표시. 실제 Edge 파일·근거·검토·900px 화면 및 초대 전달 확인.
- 큰 보고서에서 탐지 근거·누락·실행 흔적·탐지 파일 우선 순서 및 파일 중복 없음 확인. 10MB 제한 자체는 유지되며 전체 결과는 로컬 저장 필요.

아직 실제 전체 디스크 수십만 파일을 끝까지 검사하는 성능/메모리 측정, 관리자 권한 전체 실행, Windows 11·실제 배포 OAuth 검증은 하지 않았습니다. 공식 표본의 기능은 게임에서 실행하지 않고 정적으로만 검사했습니다. 프리캠·미니맵은 요청한 서버 금지 정책에 따른 치트 분류입니다. 하드웨어 변경·네트워크 재정의는 스푸핑 확정으로 처리하지 않습니다. 구체적인 수동 배포 절차·제한은 0.2 사용 안내 (이전 릴리스 기록)를 따릅니다.

## 0.1.1 연결·화면 변경 검증

- 초대 링크의 서버·코드 추출, JSON 초대 왕복, 서버 지정 후 코드만 입력, 비 HTTPS·사용자 정보 포함 URL·잘못된 경로·코드·스키마 거부: C# 기존 검사 포함 **45개 단언 통과**.
- 실제 WPF 초대 JSON 불러오기 → 동의 전 검사 버튼 비활성 → 동의·검사 → 보고서 제출 통과.
- 기본 1060×760 및 최소 900×680 화면의 결과 배치 확인. 연결·동의·진행·결과를 별도 화면으로 표시.
- Edge에서 운영자 초대 발급·파일 다운로드, 서버/코드 내용 일치, 로그인 없는 플레이어 초대 페이지 확인.
- 단계 전환 260ms, 진행 아이콘 회전, 웹 초대 카드 등장 효과. Windows 클라이언트 애니메이션 설정과 웹 prefers-reduced-motion을 따름.
- 개발 서버는 `/dev/login` 방문마다 별도 세션을 발급하도록 수정. 이전 공유 개발 세션이 로그아웃으로 무효화되던 문제 해결. 프로덕션 로그인과는 별개.

## 환경과 확인한 흐름

- Windows 10 19045 x64, .NET SDK 10.0.401, Node.js 24.19.
- Windows 앱: 코드 연결 → 수집 범위 동의 → 공식 표본 5개 정적 검사 → 보고서 제출 → 운영자 API 조회 성공.
- Edge 실제 브라우저: 로컬 로그인, 보고서 파일 목록·탐지 근거 조회, 검토 저장, 900px 화면 가로 넘침 없음.
- TypeScript 검사·Vite 프로덕션 빌드, Worker 배포 dry-run 성공. 실제 Cloudflare 배포는 아님.
- C# 생성 보고서를 서버 Zod 계약으로 검증. .NET의 시간대 오프셋·소수 초 형식을 허용.

## 탐지 표본

| 표본 | 결과 | 근거 |
|---|---|---|
| Meteor Client 0.5.8 JAR | 치트 프로그램 | 공식 배포 SHA-256 |
| sjrwatson autoclicker v1.0.0 EXE | 오토클릭·매크로 치트 프로그램 | 공식 배포 SHA-256 |
| EC-Autoclicker v1.4.1 AHK·EXE·x64 EXE, 3개 | 오토클릭·매크로 치트 프로그램 | 공식 배포 SHA-256 |
| 위 JAR·EXE를 .bin으로 이름 변경 | 탐지 | 파일 내용·해시, 이름에 의존하지 않음 |
| Meteor JAR에 표식 파일을 넣어 재압축 | 검토 필요 | 복수 클래스 경로, 정확한 해시와 구분 |
| 합성 AHK 반복 클릭 스크립트 | 매크로 치트 의심 / 검토 필요 | Loop·Click 복수 특징, 사용 맥락 미확인 |

5개 공식 파일은 실행하지 않고 정적으로만 읽었습니다. 검사 전후 해시 동일. 출처·고정 해시는 `samples/manifest.json`에 있으며 `pnpm samples`로 검증 후 내려받습니다. 최신 배포본을 자동으로 신뢰하거나 탐지 규칙을 자동 변경하지 않습니다.

일치하는 배포본은 사용자 요청에 따라 치트 프로그램으로 분류합니다. 범용 AutoHotkey 설치 여부·단일 클릭 명령만으로는 판정하지 않습니다. 알려진 정확한 해시와 일치하면 같은 파일의 범용 패턴 경고는 중복 생성하지 않습니다. 파일 보유는 게임 내 실제 사용의 증거가 아닙니다.

## 정상·오류 표본

- 실제 .NET 런타임 DLL 100개: 알려진 일치 0, 검토 필요 0.
- 합성 정상 Fabric JAR 100개: 탐지 0. Forge·NeoForge 정상 메타데이터 판독 확인.
- 단일 클릭 접근성 AHK 음성 표본: 탐지 0.
- 손상 JAR·잘못된 PE·메타데이터 타입 오류·잘못된 클래스: 오류/부분 완료로 처리.
- ZIP 경로 탈출은 추출하지 않음. 17MB 압축 항목과 256MB 초과 파일은 한도와 누락 기록.
- 중복 폴더, 사전 취소, 개인 경로 마스킹 확인.
- 실제 Windows 프로세스·BAM·Prefetch 검사기는 수집 범위를 반환. BAM·Prefetch 권한 부족은 누락으로 기록. 합성 Prefetch v30 시간 파싱·손상 입력 거부 확인.

C# 회귀 검사 37개 단언 통과. 실제 정상 DLL과 합성 JAR는 서로 다른 집합이며, 모든 정상 Minecraft 모드·런처를 대표하지 않습니다.

## API·공유 범위 검증

11개 API/계약 테스트: 일회용 코드, 다른 운영자의 조회·삭제 차단, 만료 코드·잘못된 토큰, CSRF, 미인증, 잘못된 스키마·개인 절대 경로, 청크 재시도·충돌·순서 누락, 닫힌 상태, 코드 요청 제한, 신규 접수 중단, 보관 만료 삭제, DB의 10MB 상한, 시간 형식, 과도한 청크·알 수 없는 필드, 미지원 파일 검사 제외 요청 거부.

서버는 앱이 제출한 보고서를 받습니다. 전송 해시·토큰은 손상·재전송·접근 제어용이며, 변조된 PC가 거짓 결과를 만들 수 없다고 보증하지 않습니다.

## 공개 배포 전 남은 확인

- 첫 지원 목표인 **Windows 11 x64에서 설치·실행**. 현재 실행 검증 OS는 Windows 10입니다.
- 실제 Cloudflare D1·GitHub OAuth 계정 연결, 초대 운영자 로그인, 실서비스 한도 오류 및 배포 후 삭제 확인.
- UAC 보조 프로세스 전체 흐름, 실제 압축 Prefetch의 버전별 표본, BAM 시간 연결의 추가 검증.
- 실제 정상 Fabric·Forge·NeoForge 모드, 런처·오버레이·접근성·개발 도구의 폭넓은 회귀 집합.
- 검사 중 파일 교체·권한 변경, 대규모 폴더의 메모리·응답성, 전송 중 네트워크 단절·저장 공간 부족의 실기기 검증.

내장 압축파일 재귀 검사, 전체 디스크·삭제 파일·USN·메모리·커널·상시 감시는 지원하지 않습니다. 프로세스 명령행 원문은 수집하지 않습니다. 프로세스/실행 흔적은 선택 파일과 연결 가능한 범위이며 게임 내 로드를 확인하지 않습니다. 모든 치트를 탐지하는 실전 검증 제품으로 표시하지 마세요.


# 0.4.0 업데이트 검증

- C# 회귀 108개: 기존 내용 탐지, 100개 정상 JAR·100개 정상 .NET DLL, 제외 경로 경계, 휴지통 v1/v2 메타데이터, 잔존 파일 내용 검사, USN V2 손상·이름 연결·개인 경로 마스킹·취소·이름만으로 확정하지 않는 판정.
- API 15개: 상세 공유 항목 선택·비공개 항목 차단·100개 페이지·마스킹·기존 요약 링크 유지·철회와 기존 권한/만료/업로드 검사.
- 실제 WPF 초대→동의→공식 배포 표본 검사→서버 제출, Edge 운영자 검토와 익명 상세 보고서·좁은 화면·철회 검증.
- 권장 프로필 생성 표본: 1,025개(64KiB씩) 중 게임/드라이버/임시 경로 1,000개 제외, 25개 유지. 파일 단계 3회 중앙값 304.1ms → 9.8ms. 따뜻한 파일 캐시의 의도적으로 제외 비중이 높은 생성 표본이며 실제 C 드라이브 속도 향상률이 아님. `artifacts/benchmark-profile.json`, `dotnet run --project desktop/Watchblock.Bench -- . profile`로 재현.
- **미검증:** 실제 사용자 휴지통 비우기와 관리자 권한의 NTFS 저널 연계는 생성 기록 파서 검증과 구분함. Windows 11 실기기, 외부 Cloudflare/OAuth, 새 설치본의 사용자 기존 설치 덮어쓰기는 별도 확인 필요. 기존 설치는 자동 변경하지 않음.

## 0.5.0 실서버 배포 준비 검증 (2026-09-19)

.NET 118개 검증, API 17개 + 배포 설정 2개 테스트 통과. 실제 WPF 초대·동의·표본 11개 정적 검사·보고서 제출 통과. Worker dry-run 번들 생성, 실행 의존성 감사(알려진 취약점 없음)를 확인했습니다. 자세한 변경과 남은 공개 환경 시험은 RELEASE-0.5.0.md (이전 릴리스 기록)를 참고하세요. Cloudflare 계정이 없어 실제 배포·OAuth·무료 CPU 한도·외부 PC 접속 검증은 수행하지 않았습니다.


## 0.5.1 공개 인프라·테마 검증 (2026-09-19)

Cloudflare 원격 D1 마이그레이션 3개와 Worker 배포 완료. HTTPS HTML 및 /api/health 200, 보안 헤더 적용, OAuth 미설정 상태의 검사 접수 503 차단을 확인했습니다. API·계약 17개와 배포 설정 2개 테스트, 웹 빌드, WPF 다크·화이트 전환과 저장·수정된 고정 아이콘 화면 검증 통과. GitHub OAuth 로그인·Windows 11 새 설치·타 PC 전체 흐름은 미검증입니다. 현재 사용법은 README.md를 참고하세요.


## 실제 GitHub OAuth 연결 확인 (2026-09-19)

새 주소 https://sentinel.sentinel-ac.workers.dev 에서 GitHub OAuth 로그인, Junnior123 설치자 화면, 실제 D1 검사 코드 발급을 확인했습니다. /api/health의 ready·admissionsOpen은 true입니다. 작은 폴더용 테스트 검사 1개를 만들었으며 사용자 PC 파일을 자동 검사하거나 보고서를 제출하지 않았습니다. 설치 파일의 GitHub Releases 공개와 다른 PC의 설치·제출 검증은 남아 있습니다. 비밀키는 Cloudflare Secret에만 저장했으며 소스·문서에는 포함하지 않습니다.
